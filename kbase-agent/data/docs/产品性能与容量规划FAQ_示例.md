# 产品性能与容量规划 FAQ

## 部署规模

标准版可单机部署用于评估；生产环境建议至少三节点集群以消除单点。高可用版要求数据库与对象存储独立部署，具体台数按并发与数据量测算。

## 容量测算

容量按"峰值并发 × 单请求资源占用"测算，并预留一定余量。上线前建议做一次压测，压测报告可作为扩容依据。

## 并发与限流

接口并发能力受实例规格与后端存储影响，实际配额以开通时下发的为准。突发流量可通过临时提额缓解，需提前提交申请。

## 扩容方式

支持垂直扩容（升配）与水平扩容（加节点）。水平扩容需要前置的负载均衡与共享存储，具体步骤见部署手册。
